# React Native's autolinker + Android CMake builds expect a flat node_modules
# layout. pnpm's default strict isolation breaks linking between transitive
# native modules (notably react-native-reanimated → react-native-worklets).
# Hoisted linker matches npm/yarn behaviour and lets autolink resolve symlinks
# the way the Gradle build expects.
node-linker=hoisted
